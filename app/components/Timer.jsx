var React = require('react');
var Nav = require('Nav');

var Timer = React.createClass({

		render: () => {
						
			return (

				<p>Timer component rendered </p>
			);
		}		
});

module.exports = Timer;
